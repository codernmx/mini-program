<canvas canvasId="mainCanvas" style="position:absolute;left:-{{canvasWidth}}px;width:{{canvasWidth}}px;height:{{canvasHeight}}px;"></canvas>
<view class="cropper-wrapper" style="height:{{cropperHeight}}px">
    <view>
        <image bindtap="previewImage" data-src="{{src}}" mode="aspectFit" src="{{src}}" style="width:300px;height:300px"></image>
    </view>
    <view style="font-size:13px;text-align:center;color:#f15b28;">点击图片 > 长按图片 > 可保存</view>
    <view class="footer-wrap" style="margin-top:20px;">
        <button class="link" openType="share">
            推荐给好友使用
        </button>
    </view>
</view>